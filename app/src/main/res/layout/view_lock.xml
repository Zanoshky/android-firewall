<?xml version="1.0" encoding="utf-8"?><!--
  Full-screen opaque lock overlay. Lives inside activity_main.xml on top of the
  content so the content is never visible or reachable while it is showing.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/lockOverlay"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingHorizontal="32dp"
    android:clickable="true"
    android:focusable="true"
    android:visibility="gone">

    <ImageView
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:src="@drawable/ic_lock"
        android:contentDescription="Locked"
        android:alpha="0.9"
        app:tint="@color/accent" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Firewall is locked"
        android:textColor="@color/text_primary"
        android:textSize="22sp"
        android:letterSpacing="-0.02"
        android:fontFamily="sans-serif-medium"
        android:layout_marginTop="18dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enter your PIN to continue"
        android:textColor="@color/text_secondary"
        android:textSize="14sp"
        android:layout_marginTop="4dp" />

    <EditText
        android:id="@+id/editLockPin"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:background="@drawable/bg_search"
        android:hint="PIN"
        android:textColorHint="@color/text_hint"
        android:textColor="@color/text_primary"
        android:textSize="18sp"
        android:letterSpacing="0.3"
        android:gravity="center"
        android:inputType="numberPassword"
        android:imeOptions="actionDone"
        android:maxLength="12"
        android:importantForAutofill="no"
        android:contentDescription="App lock PIN"
        android:saveEnabled="false"
        android:layout_marginTop="28dp" />

    <TextView
        android:id="@+id/txtLockError"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:textColor="@color/danger"
        android:textSize="13sp"
        android:gravity="center"
        android:layout_marginTop="10dp"
        android:minHeight="18dp" />

    <TextView
        android:id="@+id/btnUnlock"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:gravity="center"
        android:text="Unlock"
        android:textColor="@color/accent"
        android:textSize="15sp"
        android:fontFamily="sans-serif-medium"
        android:background="@drawable/bg_toggle_on"
        android:foreground="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:layout_marginTop="8dp" />

</LinearLayout>
